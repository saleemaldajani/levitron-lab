[variables]
NPM_CONFIG_CACHE = "/tmp/.npm"

[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["npm ci --cache /tmp/.npm --prefer-offline"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
